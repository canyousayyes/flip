define(['jquery', 'backbone'], function ($, Backbone) {
    var Game = {
        init: function () {
            console.log($);
            console.log(Backbone);
        }
    };
    return Game;
});
